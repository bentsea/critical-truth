# Always use www in the domain
# Force https
RewriteEngine On
RewriteCond %{HTTP_HOST} ^([a-z.]+)?critical-truth\.com$ [NC]
RewriteCond %{HTTP_HOST} !^www\. [NC]
RewriteRule .? https://www.%1critical-truth.com%{REQUEST_URI} [R=301,L]
